#!/bin/bash

# Lock the screen
pidof hyprlock || hyprlock &

# Lock Bitwarden if available (no-op if not logged in)
if command -v bw >/dev/null 2>&1; then
  bw lock >/dev/null 2>&1 &
fi

# Avoid running screensaver when locked
pkill -f "alacritty --class Screensaver"
